# test route
index:
    path: /
    controller: HomeController::show

contact:
    path: /contact
    controller: ContactController::show

newUser:
    path: /users/new
    controller: UsersController::showNew

connectUser:
    path: /users/connect
    controller: UsersController::showConnect

